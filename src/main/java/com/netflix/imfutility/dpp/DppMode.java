package com.netflix.imfutility.dpp;

/**
 * Supported DPP modes: conversion to DPP format; generation of a sample metadata.xml (input for conversion);
 * generation of a sample audio,ap.xml (input for conversion).
 */
public enum DppMode {

    convert, metadata, audiomap

}
